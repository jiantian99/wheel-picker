# 🎡 轮盘抽奖

一个移动端轮盘抽奖页面，单 HTML 文件，无需构建。

## 功能

- 添加/删除选项，生成对应轮盘
- 点击转动，动画自然减速后弹窗显示结果
- 选项自动缓存（localStorage）

## 使用

```
python3 -m http.server 8080
```

浏览器打开 `http://localhost:8080`。

也可直接双击 `index.html` 用 `file://` 打开。

## 部署

上传 `index.html` 到 GitHub Pages 仓库即可。
